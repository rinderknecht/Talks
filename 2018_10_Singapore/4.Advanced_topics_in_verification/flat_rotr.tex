%%-*-latex-*-

\documentclass{slides}

\pagestyle{empty}
\usepackage{pst-tree}
\usepackage{amsmath,amssymb}

\input{commands}

\begin{document}

\pstree[nodesep=2pt,levelsep=17pt,treesep=14pt]{\TR{\fun{flat}}}{
  \pstree{\TR{\texttt{::}}}{
    \pstree{\TR{\texttt{::}}}{
      \TR{\(x\)}
      \TR{\(s\)}
    }
    \TR{\(t\)}
  }
}
\raisebox{-3pt}{\(\xrightarrow{\smash\gamma}\)}
\pstree[nodesep=2pt,levelsep=17pt,treesep=14pt]{\TR{\fun{flat}}}{
  \pstree{\TR{\texttt{::}}}{
    \TR{\(x\)}
    \pstree{\TR{\texttt{::}}}{
      \TR{\(s\)}
      \TR{\(t\)}
    }
  }
}

\end{document}
